def average(array):
    arr_set = set(array)
    average = round(sum(arr_set)/ len(arr_set), 3)
    return average

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    result = average(arr)
    print(result)
